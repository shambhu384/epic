{% extends 'base.html.twig' %}

{% block title %}Investment{% endblock %}

{% block body %}
    <h1>Investment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ investment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ investment.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ investment.createdAt ? investment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ investment.updatedAt ? investment.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Summary</th>
                <td>{{ investment.summary }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('investment_index') }}">back to list</a>

    <a href="{{ path('investment_edit', {'id': investment.id}) }}">edit</a>

    {{ include('investment/_delete_form.html.twig') }}
{% endblock %}
